## 目标
- 将当前项目的最新更改安全、规范地推送到 GitHub ✅

## 前置检查
- 在项目目录运行：`git status`、`git branch --show-current`、`git remote -v` 📋
- 若未设置远端：`git remote add origin <repo_url>`（默认分支使用 `main`）
- 若仓库未初始化：`git init && git add . && git commit -m "chore: initial commit" && git branch -M main` 🧱

## 提交规范
- 统一使用规范化提交信息（如存在 commitlint/钩子则遵守）✍️
- 执行：`git add -A && git commit -m "feat: 更新本次改动摘要"`

## 推送操作
- 已设上游：`git push` 🚀
- 未设上游：`git push -u origin <当前分支>`
- 如远端开启保护分支：走 PR 流程（创建新分支 `feature/...` 并推送）

## 同步与冲突处理
- 远端领先：`git fetch --all` 后选择 `git pull --rebase` 或 `git rebase origin/<分支>` 🔁
- 解决冲突后：`git add -A && git rebase --continue`，最后 `git push`
- 不进行 `--force` 推送，除非你明确允许（避免覆盖历史）⚠️

## 大文件与 LFS
- 检查是否有 >100MB 文件：`git ls-files | xargs -I{} du -k {} | sort -nr | head` 📦
- 必要时添加到 `.gitignore` 或配置 Git LFS：`git lfs install && git lfs track "*.bin"`

## 认证与权限
- 使用 SSH（推荐）或 PAT；macOS 钥匙串会缓存凭据 🔐
- 如 SSH：确认 `~/.ssh/id_rsa.pub` 已添加到 GitHub 并远端为 `git@github.com:...`

## 验证结果
- 推送后检查 GitHub 仓库页面是否出现最新提交 ✅
- 本地验证：`git log --oneline --graph --decorate --all`、`git remote show origin`

## 回退策略
- 推送失败不丢本地提交，按错误信息修复后重试 ♻️
- 如误提交敏感信息：使用 `git revert` 或 `git filter-repo` 处理并再推送

## 你确认后我将执行
- 在当前项目目录依序运行上述命令，并在出现冲突/错误时按流程处理，最终完成推送并回报结果📣